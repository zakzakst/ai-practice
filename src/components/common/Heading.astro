---
import "@styles/common/heading.css";

interface Props {
  tag?: string;
}
const { tag } = Astro.props;
const Tag = tag || "h3";
---

<!-- TODO: tsエラーの解消方法調べる -->
<Tag class="heading">
  <span class="heading__mark"></span>
  <slot />
</Tag>
