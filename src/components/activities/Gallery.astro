---
import "@styles/activities/gallery.css";
import Heading from "@components/common/Heading.astro";
import { getBasePath } from "@lib/utils";

type Item = {
  thumbnail: string;
  caption: string;
};
const items: Item[] = [
  {
    thumbnail: "gallery-01",
    caption: "練習メニューの様子",
  },
  {
    thumbnail: "gallery-02",
    caption: "体育館での活動風景",
  },
  {
    thumbnail: "gallery-03",
    caption: "練習試合の様子",
  },
];
---

<section>
  <Heading>活動風景</Heading>
  <div class="mt-4">
    <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      {
        items.map((item) => (
          <li>
            <div>
              <img
                src={getBasePath(`/img/activities/${item.thumbnail}.webp`)}
                alt=""
                loading="lazy"
              />
            </div>
            <p class="mt-1 font-bold text-center">{item.caption}</p>
          </li>
        ))
      }
    </ul>
  </div>
</section>
